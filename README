This program converts a .ppm file into an ASCII Art .txt file.

Compile:
gcc -g -o ppm ppm.c

Run:
./ppm <input_ppm_file> <output_txt_file> <0/1>

where 0/1 determines whether contrast is applied to the image colors or not.
